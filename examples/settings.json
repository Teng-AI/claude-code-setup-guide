{
  "model": "opus",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -qE 'git commit|git push'; then staged=$(git diff --cached --name-only 2>/dev/null); if [ -n \"$staged\" ]; then echo 'Reminder: Run /harden, /test-gaps, and /docs-sync before committing if you have not already.'; fi; fi; exit 0"
          },
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -qE 'git push.*( -f($| )| --force)'; then echo 'Blocked: force-push from the agent is not allowed. Do it yourself if you really mean it.'; exit 2; fi; if echo \"$cmd\" | grep -qE 'git push.*( |:)(main|master)($| )'; then authors=$(git log --format='%ae' 2>/dev/null | sort -u | wc -l | tr -d ' '); if [ \"${authors:-0}\" -gt 1 ]; then echo 'Blocked: this repo has multiple contributors - do not push directly to main/master. Use a feature branch + PR.'; exit 2; fi; fi; exit 0"
          },
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -qiE 'vercel deploy|netlify deploy|firebase deploy|npm run deploy|fly deploy'; then echo 'Deploy detected. Run /pre-ship first if you have not already.'; fi; exit 0"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "res=$(jq -r '(.tool_response // .tool_result // empty) | if type==\"string\" then . else tojson end' 2>/dev/null | head -c 800); if echo \"$res\" | grep -qiE 'error|failed|exception'; then echo 'Error detected. Consider running /debug for systematic troubleshooting.'; fi; exit 0"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/pre-compact.sh",
            "timeout": 20000
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/session-start.sh",
            "timeout": 10000
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "prompt=$(jq -r '.prompt // \"\"' | tr '[:upper:]' '[:lower:]'); if echo \"$prompt\" | grep -qE 'payment|billing|stripe|auth(entication)?|login|signup|password|prod(uction)?.?data|migration'; then echo 'High-stakes task detected. Consider running /pre-implement + /pre-mortem before coding.'; fi; exit 0"
          },
          {
            "type": "command",
            "command": "prompt=$(jq -r '.prompt // \"\"' | tr '[:upper:]' '[:lower:]'); if echo \"$prompt\" | grep -qE 'never used|first time|unfamiliar|new to|don.t know how|haven.t worked with'; then echo 'Unfamiliar territory detected. Consider running /learn before implementing.'; fi; exit 0"
          },
          {
            "type": "command",
            "command": "prompt=$(jq -r '.prompt // \"\"' | tr '[:upper:]' '[:lower:]'); if echo \"$prompt\" | grep -qE 'still (not|broken|failing)|same error|tried everything|going in circles'; then echo 'Debugging loop detected. Consider running /fresh-eyes for a reset.'; fi; exit 0"
          }
        ]
      }
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "sh ~/.claude/statusline-command.sh"
  },
  "alwaysThinkingEnabled": true,
  "effortLevel": "xhigh",
  "autoUpdatesChannel": "latest",
  "skipDangerousModePermissionPrompt": false,
  "inputNeededNotifEnabled": true,
  "agentPushNotifEnabled": true,
  "voiceEnabled": true
}
